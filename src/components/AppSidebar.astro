---
import { displayCourseTitle } from '../lib/grades/display';

interface Props {
	courses?: { title: string }[];
	periodQuery?: string;
	activeIndex?: number;
	section?: 'grades' | 'documents' | 'attendance' | 'mail';
}

const { courses = [], periodQuery = '', activeIndex, section = 'grades' } = Astro.props;

const itemClass = (active: boolean) => [
	'flex items-center gap-2 rounded-md px-2 py-1.5 text-sm no-underline',
	active
		? 'bg-muted font-medium text-foreground'
		: 'text-muted-foreground hover:bg-muted/60 hover:text-foreground',
];
---

<aside class="sticky top-0 hidden h-svh w-60 shrink-0 flex-col border-r border-border bg-background md:flex" aria-label="App">
	<div class="flex min-h-0 flex-1 flex-col overflow-y-auto px-2 py-3">
		<a
			href={`/grades${periodQuery}`}
			data-grades-home
			class="mb-3 inline-flex size-9 items-center justify-center rounded-md no-underline hover:bg-muted/60"
			aria-label="Grade Viewer"
		>
			<img src="/favicon.svg" alt="" class="size-7 rounded" width="28" height="28" />
		</a>
		<nav class="flex min-h-0 flex-1 flex-col gap-0.5" aria-label="Sections">
			<a
				href="/mail"
				class:list={itemClass(section === 'mail')}
				aria-current={section === 'mail' ? 'page' : undefined}
			>
				<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-4 shrink-0" aria-hidden="true">
					<path d="M2.992 16.342a2 2 0 0 1 .094 1.167l-1.065 3.29a1 1 0 0 0 1.236 1.168l3.413-.998a2 2 0 0 1 1.099.092 10 10 0 1 0-4.777-4.719"></path>
				</svg>
				Mail
			</a>
			<a
				href="/attendance"
				class:list={itemClass(section === 'attendance')}
				aria-current={section === 'attendance' ? 'page' : undefined}
			>
				<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-4 shrink-0" aria-hidden="true">
					<path d="M8 2v4"></path>
					<path d="M16 2v4"></path>
					<rect width="18" height="18" x="3" y="4" rx="2"></rect>
					<path d="M3 10h18"></path>
				</svg>
				Attendance
			</a>
			<div class="min-h-0">
				<a
					href={`/grades${periodQuery}`}
					data-grades-home
					class:list={itemClass(section === 'grades' && activeIndex == null)}
					aria-current={section === 'grades' && activeIndex == null ? 'page' : undefined}
				>
					<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-4 shrink-0" aria-hidden="true">
						<path d="M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H19a1 1 0 0 1 1 1v18a1 1 0 0 1-1 1H6.5a1 1 0 0 1 0-5H20"></path>
						<path d="M8 11h8"></path>
						<path d="M8 7h6"></path>
					</svg>
					Grades
				</a>
				<div id="course-nav" class="mt-0.5 ml-4 border-l border-border pl-1.5" aria-label="Classes">
					<ul class="flex flex-col gap-px">
						{courses.map((course, index) => {
							const title = displayCourseTitle(course.title);
							const active = section === 'grades' && index === activeIndex;
							return (
								<li class="min-w-0">
									<a
										href={`/grades/${index}${periodQuery}`}
										class:list={[
											'block truncate rounded-md px-2 py-1.5 text-sm no-underline',
											active
												? 'bg-muted font-medium text-foreground'
												: 'text-muted-foreground hover:bg-muted/60 hover:text-foreground',
										]}
										aria-current={active ? 'page' : undefined}
										title={title}
									>
										{title}
									</a>
								</li>
							);
						})}
					</ul>
				</div>
			</div>
			<a
				href="/documents"
				class:list={itemClass(section === 'documents')}
				aria-current={section === 'documents' ? 'page' : undefined}
			>
				<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="size-4 shrink-0" aria-hidden="true">
					<path d="M6 22a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h8a2.4 2.4 0 0 1 1.704.706l3.588 3.588A2.4 2.4 0 0 1 20 8v12a2 2 0 0 1-2 2z"></path>
					<path d="M14 2v5a1 1 0 0 0 1 1h5"></path>
					<path d="M10 9H8"></path>
					<path d="M16 13H8"></path>
					<path d="M16 17H8"></path>
				</svg>
				Documents
			</a>
		</nav>
	</div>
</aside>
