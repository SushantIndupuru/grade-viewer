# Replace the hostname with a DNS name that points at this VM, then reload Caddy.
# sslip.io works without extra DNS: <public-ip>.sslip.io

{
	email contact@gradeviewer.org
}

192.9.145.215.sslip.io {
	reverse_proxy 127.0.0.1:5001
}
